@model List<(string Title, string Message)>
@{
    ViewData["Title"] = "Announcements";
}

<h2>📢 Municipal Announcements</h2>

@foreach (var item in Model)
{
    <div class="card mb-3">
        <div class="card-body">
            <h5 class="card-title">@item.Title</h5>
            <p class="card-text">@item.Message</p>
        </div>
    </div>
}

<a href="/Home/AdminLogin" class="btn btn-outline-primary mt-3">Login to Post</a>